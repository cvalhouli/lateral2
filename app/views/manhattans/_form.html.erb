<%= form_for(@manhattan) do |f| %>
  <% if @manhattan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manhattan.errors.count, "error") %> prohibited this manhattan from being saved:</h2>

      <ul>
      <% @manhattan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
