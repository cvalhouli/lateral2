<%= form_for @picture, :html => { :multipart => true } do |f| %>
   <%=f.file_field :photo %>
  <div class="field">
    <%= f.hidden_field :property_id, :value=>params[:q] %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
